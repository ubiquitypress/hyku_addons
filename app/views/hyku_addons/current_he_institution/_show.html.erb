<!-- rendered from views/hyrax/base///_attribute_rows.html.erb -->
<% array_of_hash = get_model(presenter.current_he_institution, presenter.model, 'current_he_institution', nil) %>

<% if (array_of_hash.class == Array && array_of_hash.first.class == Hash)  %>
  <%= render "hyku_addons/current_he_institution/show_array_hash",  array_of_hash: array_of_hash %>
<% else %>

<% end %> <%# closes if array_of_hash.class %>
