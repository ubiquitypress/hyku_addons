<!-- rendered from views/hyrax/base///_attribute_rows.html.erb -->

<% array_of_hash = get_model(presenter.contributor, presenter.model, 'contributor', nil) %>

 <% if (array_of_hash.class == Array && array_of_hash.first.class == Hash )  %>

   <%= render "hyku_addons/contributor/show_array_hash", array_of_hash: array_of_hash  %>
<% else %>

<% end %> <%# closes if /else array_of_hash.class %>
