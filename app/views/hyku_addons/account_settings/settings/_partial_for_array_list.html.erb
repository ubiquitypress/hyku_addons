<!-- This partial is shared by the three field (weekly_email, monthly_email, yearly_email) called form each individual filed partial
-weekly_email_list.htm.erb
-monthly_email_list.htm.erb
-yearly_email_list.htm.erb
-->
<div class="row">
  <div class="col-md-12">

    <div class="panel panel-default account-form">

        <%= form_for(account, url: hyku_addons.update_single_admin_account_setting_path(account), :html => { class: 'form control' }) do |f| %>
        <div class="panel-body">
          <% if @account.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>
              <ul>
                <% @account.errors.messages.each do |key, messages| %>
                  <li><%= key %> &quot;<%= @account.errors.details[key].first[:value] %>&quot; <%= messages.join(' and ') %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          
          <div class="form-group">
            <%=f.fields_for :settings do |ff| %>
              <%= ff.label field_name %><br>
              <%= ff.text_field field_name.to_sym, :value => @account.settings[field_name],
               name: "#{f.object_name}[settings][#{html_name_element}][]", class: 'form-control' %><br>
            <% end %>
         </div>
         <div class="panel-footer">
          <%= f.submit class: 'btn btn-primary pull-right' %> <br>
          <h1> <%= link_to "Back",  hyku_addons.admin_account_settings_path, class: "btn btn-xs btn-default  pull-right" %></h1>

         </div>
       <% end %>
    </div>

  </div>
</div>