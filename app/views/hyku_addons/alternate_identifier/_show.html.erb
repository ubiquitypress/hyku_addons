
<% array_of_hash = get_model(presenter.alternate_identifier, presenter.model, 'alternate_identifier') %>

<% if (array_of_hash.class == Array && array_of_hash.first.class == Hash )  %>
  <%= render "hyku_addons/alternate_identifier/show_array_hash", array_of_hash: array_of_hash  %>
<% else %> <!-- handle no record case -->
<% end %> <%# closes if /else array_of_hash.class %>
