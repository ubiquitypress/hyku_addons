<% array_of_hash = get_model(presenter.related_identifier, presenter.model, 'related_identifier') %>

<% if (array_of_hash.class == Array && array_of_hash.first.class == Hash )  %>
  <%= render "hyku_addons/related_identifier/show_array_hash", array_of_hash: array_of_hash  %>
<% end %>
