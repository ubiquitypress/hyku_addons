<!-- _<%= key %> -->
<% if f.object.schema_driven? %>
  <%= render "records/edit_fields/default_date", f: f, key: key %>

<% else %>
  <%= render "records/edit_fields/legacy_cloneable_date", field_slug: :related_exhibition_date, f: f %>
<% end %>
