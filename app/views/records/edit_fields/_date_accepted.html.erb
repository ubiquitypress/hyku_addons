<!-- _<%= key %> -->
<% if f.object.schema_driven? %>
  <%= render "records/edit_fields/default_date", f: f, key: key %>

<% else %>
  <%= render "hyku_addons/dates/date_accepted", f: f, date_accepted: f.object[:date_accepted] %>
<% end %>
