<%= f.input :resource_type, as: :select,
            collection: HykuAddons::ResourceTypesService.new(model: f.object.model.class).select_active_options,
            include_blank: true,
            input_html: { class: 'form-control' } %>
