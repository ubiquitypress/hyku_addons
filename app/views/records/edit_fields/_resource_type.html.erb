<%= f.input :resource_type, as: :multi_value_select,
            collection: HykuAddons::ResourceTypesService.new(model: f.object.model.class, request: request).select_active_options,
            include_blank: true,
            input_html: { class: 'form-control' } %>

