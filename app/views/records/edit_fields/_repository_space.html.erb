<% # This file can be removed when the schema migration has been completed %>

<% if f.object.schema_driven? %>
  <%= render "records/edit_fields/default", f: f, key: key  %>

<% else %>
  <%= f.input :repository_space, as: :select,
    collection: HykuAddons::RepositorySpaceService.new(model: f.object.model.class).select_active_options,
    include_blank: false,
    input_html: { class: 'form-control' } %>
<% end %>
