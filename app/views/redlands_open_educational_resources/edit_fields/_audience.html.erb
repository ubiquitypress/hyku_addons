<%= f.input :audience, as: :select,
            collection: HykuAddons::AudienceService.new(model: f.object.model.class).select_active_options,
            include_blank: false,
            input_html: { class: 'form-control' } %>
